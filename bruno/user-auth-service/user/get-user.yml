info:
  name: get-user
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/v1/users/username"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
