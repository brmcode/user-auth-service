info:
  name: delete-user
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/v1/users/username"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
