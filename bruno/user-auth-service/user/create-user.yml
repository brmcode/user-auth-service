info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/users"
  body:
    type: json
    data: |-
      {
          "email": "{{$randomExampleEmail}}",
          "first_name": "{{$randomFirstName}}",
          "last_name": "{{$randomLastName}}",
          "password": "123456",
          "role": "ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
