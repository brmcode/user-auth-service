info:
  name: update-user
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/api/users/username"
  body:
    type: json
    data: |-
      {
        "username": "jpmqwgyi",
        "first_name": "{{$randomFirstName}}",
        "last_name": "{{$randomLastName}}",
        "role": "USER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
