info:
  name: get-image
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/media/image/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
